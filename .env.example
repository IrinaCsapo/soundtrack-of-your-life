# Replicate — music + cover generation
REPLICATE_API_TOKEN=

# Anthropic — music prompt + visual prompt + title generation + moderation
ANTHROPIC_API_KEY=

# Supabase — persistence (database + storage for audio + covers)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Admin — secret password used to access /admin to delete or hide soundtracks
ADMIN_PASSWORD=

# Base URL — used by the OG image + canonical URLs
# NEXT_PUBLIC_BASE_URL=https://soundtrack.irina.love
