# Replicate — music generation
REPLICATE_API_TOKEN=

# Supabase (DB + storage) — add when you set it up
# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (title generation) — add when ready
# ANTHROPIC_API_KEY=

# SoundCloud (opt-in publishing) — add when ready
# SOUNDCLOUD_CLIENT_ID=
# SOUNDCLOUD_CLIENT_SECRET=
# SOUNDCLOUD_REFRESH_TOKEN=

# Base URL — used by the Replicate webhook callback in production
# NEXT_PUBLIC_BASE_URL=https://irina.love
